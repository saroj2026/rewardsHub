import { PlaceholderScreen } from "./PlaceholderScreen";

export function CardsScreen() {
  return <PlaceholderScreen title="Cards" subtitle="Wallet — linked cards, reward balances, card details." />;
}
